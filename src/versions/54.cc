#include "../LuaState.h"

// Napi::Value LuaState::GC54(const Napi::CallbackInfo& info) {
//     lua_gc(L);
// }

// Napi::Value LuaState::GetField54(const Napi::CallbackInfo& info) {
//     return Napi::Number::New(info.Env(), lua_getfield(L, GetArg<int>(info, 0), GetArg<const char*>(info, 1)));
// }
